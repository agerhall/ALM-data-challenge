class spectrumKernel:

    def __init__(self, order, missmatch):
        self.order=order
        self.missmatch= missmatch

    def trie(self,data, order):


    def compute(self, data):

        return